# Local env (RTX 2060, micromamba `def`). Exact versions this repo was run with.
# Colab installs its own torch — see README "Training on Colab".
# NOTE: accelerate==0.33.0 is BELOW trl==0.24.0's own floor (accelerate>=1.4.0)
# -- SFTTrainer/GRPOTrainer fail to import under this pin (confirmed: `from
# trl import SFTConfig` raises `TrainingArguments has no attribute
# _VALID_DICT_FIELDS`). Never hit in practice because real training runs on
# Colab, not `def` -- `def` is data/eval-smoke only. Not bumping here since
# v3's GRPO stage needs a much newer trl anyway (see requirements-grpo.txt);
# fixing this pin in place would just be re-deriving that same upgrade twice.
torch==2.10.0
transformers==4.50.3
peft==0.19.1
trl==0.24.0
datasets==2.19.2
bitsandbytes==0.49.2
accelerate==0.33.0
numpy==1.26.4
rdkit==2023.9.6   # conda build reports 2023.09.6; pip wheel name is `rdkit`
requests           # PubChem PUG-REST (rdkit_qa_v3/name_dict_pubchem.py, build_name_dict.py)
